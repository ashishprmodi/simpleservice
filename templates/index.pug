html
    head
    body(style=`text-align: center; color: white; background-color: ${backgroundColor};`)
        h1(style="padding-top: 5rem")= heroText
        h2= version
        h3 running on host #{host}
        if showButton
            button(onclick='handleClick()' style='padding: 12px; font-size: 16px;') Click me
    script(src='/static/app.js')
